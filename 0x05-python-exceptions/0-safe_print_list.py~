#!/usr/bin/python3
def safe_print_list(my_list=[], x=0):
    cont = 0;
    try:
        for value in range(x):
            print({}.format(my_list[value]))
            cont += 1;
    except:
        pass
    print()
    return(cont)
